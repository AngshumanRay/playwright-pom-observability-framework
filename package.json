{
  "name": "playwright-observability-framework",
  "version": "1.0.0",
  "private": true,
  "description": "Custom Playwright framework with Page Object Model, metrics instrumentation, and Universal Report.",
  "scripts": {
    "clean:reports": "node -e \"const fs=require('fs');['Reports','artifacts'].forEach(p=>fs.rmSync(p,{recursive:true,force:true}));\"",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report:playwright": "playwright show-report Reports/playwright-html",
    "reports": "sh -c 'npm run clean:reports; npm run test; exit $?'",
    "report:open:all": "sh -c 'npx playwright show-report Reports/playwright-html & open Reports/universal-report/index.html'"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@types/node": "^22.13.10",
    "typescript": "^5.7.3"
  }
}
